<?xml version="1.0"?>
<ruleset name="PSR-12 Standard">
    <description>Check for PSR-12 compliance.</description>

    <rule ref="PSR12" />

    <!-- Directories to include -->
    <file>src</file>
    <file>tests</file>

    <!-- Directories or files to exclude -->
    <exclude-pattern>vendor</exclude-pattern>
</ruleset>
